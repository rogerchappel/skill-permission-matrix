# Examples

Run a review with a local policy config:

```bash
node ../dist/src/cli.js scan ../fixtures/skills --format markdown --config review-config.json
```

Use JSON output for release evidence:

```bash
node ../dist/src/cli.js scan ../fixtures/skills --format json --config review-config.json
```
