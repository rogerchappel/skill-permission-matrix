# Contributing

Keep changes local-first and deterministic. New extraction rules should include fixture coverage and a short note in `docs/WARNINGS.md` when they create new warning text.

Before opening a PR, run:

```bash
npm run validate
```
